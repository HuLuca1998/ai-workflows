-- 节点在**当时**的标题。
--
-- 没有它的话失败横幅只能显示内部 id（「节点『script_shell_2』失败」），
-- 而用户从没见过那个 id —— 他看到的是自己起的名字「解析日志」。
--
-- 记在事件里而不是让界面现查图：草稿随时可能改名，
-- 而运行记录说的是**当时**发生了什么。这也是「RunEvent 是唯一事实来源」
-- 的一部分 —— 一条运行记录要能独立解释自己，不依赖当前的草稿状态。
ALTER TABLE run_event ADD COLUMN node_label TEXT;
