[package]
name = "aiwf-mcp"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
aiwf-core-api = { path = "../core-api" }
aiwf-engine = { path = "../engine" }
aiwf-store = { path = "../store" }
serde = { workspace = true }
serde_json = { workspace = true }
tiny_http = "0.12"

[dev-dependencies]
tempfile = "3.27.0"
